# MonarchMoney TypeScript Library - Test Environment
# Copy this file to .env and fill in your actual credentials

# Required: Your MonarchMoney login credentials
MONARCH_EMAIL=your-email@example.com
MONARCH_PASSWORD=your-secure-password

# Optional: If you have MFA enabled and want to use TOTP secret
# You can get this from MonarchMoney Settings > Security > MFA setup
# MONARCH_MFA_SECRET=your-totp-secret-key

# Optional: API configuration
# MONARCH_BASE_URL=https://api.monarchmoney.com

# Optional: Logging level (debug, info, warn, error)
MONARCH_LOG_LEVEL=info

# Optional: Cache encryption key (will be generated if not provided)
# MONARCH_CACHE_ENCRYPTION_KEY=your-32-character-encryption-key

# Optional: Pre-shared session token (if you already have one)
# MONARCH_SESSION_TOKEN=your-existing-session-token